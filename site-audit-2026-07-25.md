# Аудит MpHub от 2026-07-25

Проверка выполнена по production `wb-site`, активный путь `/home/makson/current -> /home/makson/releases/20260718-090353`, приложение `pm2 mphub` online. Время проверки: 2026-07-25, около 11:15-11:45 МСК.

## Методика

- Обновил `SESSION_STATE.md` через `npm run save-session-state`.
- Сверил карту меню по `src/components/Sidebar.tsx`.
- Сверил ожидания по `PROJECT_CONTEXT.md`, `TODO.md`, `public/data/docs.json`.
- Создал временный audit-cookie на production из runtime `JWT_SECRET`, без вывода токена/секретов.
- Проверил production API через `http://127.0.0.1` на сервере.
- Проверил production UI браузером через SSH-туннель `127.0.0.1:18080 -> wb-site:80`.
- Проверил PostgreSQL-свежесть данных и cron-логи.
- Мутации не запускал: не удалял API-ключи, не отправлял жалобы, не запускал ручные WB-загрузки, не сохранял настройки.

Артефакты проверки:

- `/tmp/mphub-api-audit.json` на `wb-site`
- `/tmp/mphub-db-audit.json` на `wb-site`
- `/private/tmp/mphub-ui-audit.json`
- `/private/tmp/mphub-click-audit.json`

## Главная сводка

✅ Основные разделы открываются: аналитика, финансы, отгрузка, логистика, склад, поставки, отчёты поставок, закупки, отзывы, настройки, база знаний.

✅ Все проверенные production API с валидной авторизацией вернули `200 OK`.

✅ Основные данные актуальны:

- Отгрузка: `shipment_products=63`, `shipment_stock=804`, остаток `28 494 шт`, обновлено `2026-07-25T08:23:10Z` (11:23 МСК).
- Заказы: `shipment_orders=252 362`, max date `2026-07-25T11:12:12`, за 28 дней `36 064`.
- Orders funnel: max date `2026-07-24`, 28 дней `36 701 заказ`, `45 539 582 ₽`.
- Финансы: `realization=3 370 079`, max `rr_dt=2026-07-24`, max `sale_dt=2026-07-24`.
- Реклама: max date `2026-07-24`.
- Хранение: max date `2026-07-24`.
- Склад Google Sheets: `15 369 шт`, `177.12 коробов`, последний импорт `2026-07-24T16:31:53Z`.
- Логистика: объёмы остатков обновлены `2026-07-25T06:00:11Z`, замеры `2026-07-25T06:10:01Z`.
- Отзывы: `157 858`, max review date `2026-07-25`, sync updated `2026-07-25 11:30:02+03`.
- Принятые поставки: `308`, max supply date `2026-07-25T00:30:03+03`.

❌ Найдены проблемы:

1. `/changelog` сломан: страница зависает на `Загрузка журнала...`, потому что на production отсутствует `public/data/changelog.json`.
2. `/monitor` показывает красный статус `Cron автожалобы: Нет успешных запусков`, хотя cron фактически запускается каждые 30 минут. Причина: мониторинг считает успешным не тот паттерн лога; текущий штатный итог при выключенных автожалобах: `No accounts with auto_complaints enabled`.
3. В мониторинге карточка WB auth показывает `API — / ЛК — не проверялось`, хотя `data/auth-check.log` за `2026-07-24 22:00 МСК` содержит `API: ok`, `LK: ok`, `All channels ok`. Нужно проверить источник `auth-status.json`.

⚠️ Предупреждения:

1. `/api/supplies?limit=20` работает, но ответ занял `29.9 сек`. UI открылся быстрее за счёт текущего состояния/кэша, но live API поставок остаётся тяжёлым.
2. В `shipment_stock` есть `19` баркодов без заданной себестоимости в `cogs_history`. Это может искажать юнит-экономику/себестоимость по части товаров.
3. В логистике есть `8` критичных замеров WB, где measurement выше карточного объёма и не перекрыт отчётом остатков.
4. В `review_accounts` `cookie_status=inactive`, но глобальная WB-сессия в настройках активна. Для текущего режима это не ломает отзывы, потому что автоответы/автожелобы выключены, но при включении автоматик лучше перепроверить account-level cookie status.

## Вкладки

### 1. Аналитика

Назначение: показывает продажи/заказы/доставки/отказы/выкупы, остатки, региональное распределение, матрицу локальности.

Источники:

- `/api/data/products`
- `/api/data/stock`
- `/api/data/orders-aggregated?days=28`
- `/api/data/meta`
- `/api/data/buyout-rates`
- PostgreSQL: `shipment_products`, `shipment_stock`, `shipment_orders`, `orders_funnel`.

Проверка:

- ✅ UI открылся за `13.4 сек`, API ошибок нет.
- ✅ Товары: `63`.
- ✅ Остатки: `28 494 шт`.
- ✅ Заказы за период на UI: `39 546`; детализация `38 651 из 39 546`.
- ✅ API агрегатов за 28 дней: `35 818 totalOrders`, `30 305 totalNonCancelled`, все 8 ФО есть.
- ✅ Данные обновлены `25.07.2026, 11:24`.
- ⚠️ Есть расхождение агрегата UI/детализации, но UI прямо показывает покрытие детализации. Это ожидаемая диагностическая ситуация, не падение.

Статус: ✅ работает, данные актуальны.

### 2. Отзывы

Назначение: основная таблица отзывов, графики отзывов/жалоб, фильтры, статусы, ручная подача жалоб.

Источники:

- `/api/reviews`
- `/api/reviews/stats`
- `/api/reviews/sync-status`
- `/api/reviews/complaints`
- PostgreSQL: `reviews`, `review_complaints`, `sync_status`, `reviews_archive_sync_state`.

Проверка:

- ✅ UI открылся за `8.4 сек`, API ошибок нет.
- ✅ Таблица отзывов есть, 100 строк в DOM.
- ✅ Всего отзывов API: `157 858`.
- ✅ Max review date: `2026-07-25`.
- ✅ Reviews sync работает: последний лог `2026-07-25T08:30:02Z`, штатное ожидание WB лимита до `2026-07-25T08:44:07Z`.
- ✅ График отзывов показывает данные.
- ✅ График жалоб показывает `Нет данных о жалобах`; это корректно для текущего периода: последние жалобы созданы `2026-06-09`.
- ✅ В таблице доступны кнопки `Жалоба`.

Статус: ✅ работает, данные актуальны.

### 2.1 Автоответы

Назначение: настройки автоматических ответов по оценкам, подпись.

Источники:

- `/api/reviews/accounts`
- `review_accounts.settings_json`.

Проверка:

- ✅ UI открылся за `4.7 сек`, API ошибок нет.
- ✅ Аккаунт выбран: `ИП Белякова А. Л. — IMSI`.
- ✅ Форма автоответов отображается.
- ✅ Кнопка `Сохранить все настройки` есть.
- ✅ Автоответы сейчас выключены.

Статус: ✅ работает как форма настроек. Мутационное сохранение не выполнялось.

### 2.2 Автожелобы

Назначение: настройки автоматической подачи жалоб на негативные отзывы, пауза при отклонениях.

Источники:

- `/api/reviews/accounts`
- `/api/reviews/complaint-status`
- `/api/reviews/complaints`
- `review_complaints`, `review_complaint_pauses`.

Проверка:

- ✅ UI открылся за `5.0 сек`, API ошибок нет.
- ✅ Показано: `Автожалобы без активной паузы`.
- ✅ За 24 часа: `одобрено 0, отклонено 0, всего 0`.
- ✅ Автожелобы выключены.
- ✅ Cron фактически запускается каждые 30 минут.
- ⚠️ Мониторинг ошибочно считает cron красным, потому что не принимает `No accounts with auto_complaints enabled` как штатный успешный итог.

Статус: ✅ функционал выключен и не падает; ❌ мониторинг этого cron некорректен.

### 2.3 Подключение WB для отзывов

Назначение: account-level WB параметры для отзывов, API/cookie/proxy, статус базы.

Источники:

- `/api/reviews/accounts`
- `/api/reviews/sync-status`
- `review_accounts`, `sync_status`.

Проверка:

- ✅ UI открылся за `4.8 сек`, API ошибок нет.
- ✅ Account: `ИП Белякова А. Л. / IMSI`, supplier `1166225`.
- ✅ API-ключ активен.
- ✅ authorizev3 активен.
- ✅ wbx-validation-key активен.
- ✅ База отзывов: `157 858`.
- ⚠️ `review_accounts.cookie_status=inactive`, но на UI токены активны. Требует отдельной сверки, если будем включать auto-функции.
- ⚠️ Кнопка `Полная загрузка` присутствует, а в базе знаний указано, что full sync через API отключён в PostgreSQL-режиме. Это потенциально вводит в заблуждение.

Статус: ✅ текущий режим работает; ⚠️ есть UX/статусные несостыковки.

### 2.4 Аккаунты отзывов

Назначение: список WB-аккаунтов и их статусы.

Источники:

- `/api/reviews/accounts`
- `review_accounts`.

Проверка:

- ✅ UI открылся за `4.7 сек`.
- ✅ Есть 1 аккаунт: `ИП Белякова А. Л. / IMSI`.
- ✅ API, authorizev3, validation-key отображаются зелёными.
- ✅ Кнопка добавления аккаунта есть.
- ⚠️ Автоответы/автодиалоги/автожелобы выключены.

Статус: ✅ работает.

### 3. Финансы

Назначение: P&L, юнит-экономика, сверка API/Excel, прогноз прибыли, настройки себестоимости/налогов/формул.

Источники:

- `/api/finance/pnl`
- `/api/finance/articles`
- `/api/finance/reconciliation`
- `/api/finance/forecast`
- `/api/finance/cogs`
- `/api/finance/barcodes`
- `/api/finance/tax-settings`
- PostgreSQL: `realization`, `advertising`, `paid_storage`, `weekly_rows`, `reports`, `cogs_history`, `orders_funnel`.

#### 3.1 Отчёты

Проверка:

- ✅ UI открылся за `5.9 сек`, API ошибок нет.
- ✅ Период UI: `25.06.2026 — 24.07.2026`.
- ✅ Реализация UI: `40 214 220 ₽`.
- ✅ Операционная прибыль UI: `4 154 011 ₽`.
- ✅ API за июль `01.07-24.07` отдаёт P&L, `sales_qty=26 960`, `net_qty=26 869`.
- ✅ Финансовые данные свежие до `2026-07-24`.

Статус: ✅ работает.

#### 3.2 Артикулы

Проверка:

- ✅ UI открылся за `5.7 сек`, таблица есть, 22 строки.
- ✅ API за июль вернул 21 артикул.
- ✅ Основные колонки есть: артикул WB, артикул продавца, продажи, выручка, реклама, маржа, прибыль/шт.
- ⚠️ В таблице есть статусы `❌` по нескольким ключевым артикулам. Это бизнес-сигнал по маржинальности/порогам, не падение UI.

Статус: ✅ работает.

#### 3.3 Сверка

Проверка:

- ✅ UI открылся за `6.7 сек`, 2 таблицы, 36 строк.
- ✅ API вернул 24 недели.
- ✅ UI показывает итог: API `205 055 164 ₽`, Excel `205 083 851 ₽`, расхождение `-28 687 ₽ (0.0%)`.
- ⚠️ Последние недели `13.07-19.07`, `06.07-12.07` и ряд других отмечены `⏳`; это означает неполную пару API/Excel, не ошибку загрузки страницы.
- ✅ `weekly_import_status` последний `ok`, updated `2026-07-22T20:12:03Z`.

Статус: ✅ работает; ⚠️ последние недели ожидают финальных данных.

#### 3.4 Прогноз

Проверка:

- ✅ UI открылся за `6.5 сек`, таблица есть, 30 строк.
- ✅ Прогноз использует текущую формулу без сценария доп-комиссии.
- ✅ UI: `39 546 заказов`, прогноз выручки `38 149 095 ₽`, реклама `2 139 344 ₽`, прогноз прибыли `3 838 873 ₽`.
- ✅ API за июль вернул 24 дня, meta содержит `articlesCount=...`, fallback-артикулы присутствуют.

Статус: ✅ работает.

#### 3.5 Себестоимость

Проверка:

- ✅ `/finance/settings` открылся за `5.6 сек`, таблица есть.
- ✅ `/api/finance/cogs` вернул 471 строку себестоимости.
- ✅ `/api/finance/barcodes` вернул 293 баркода.
- ⚠️ SQL: `19` баркодов, которые есть в `shipment_stock`, не имеют положительной себестоимости в `cogs_history`.

Статус: ✅ форма работает; ⚠️ данные себестоимости неполные.

#### 3.6 Налоги

Проверка:

- ✅ `/finance/taxes` открылся за `4.4 сек`.
- ✅ `/api/finance/tax-settings`: НДС `5`, УСН `1`.

Статус: ✅ работает.

#### 3.7 Формулы

Проверка:

- ✅ `/finance/formulas` открылся за `3.5 сек`.
- ✅ Страница справочная, API ошибок нет.

Статус: ✅ работает.

### 4. Расчёт отгрузки

Назначение: расчёт потребности к отгрузке/упаковке с учётом заказов, остатков WB, склада, выкупа, тренда и выбранных поставок.

Источники:

- `/api/data/products`
- `/api/data/stock`
- `/api/data/orders-aggregated`
- `/api/warehouse/stock`
- `/api/settings`
- `/api/overrides`
- `/api/supplies`
- PostgreSQL: `shipment_products`, `shipment_stock`, `shipment_orders`, `warehouse_ready_stock`, `user_settings`, `wb_supply_snapshots`.

#### 4.1 Расчёт

Проверка:

- ✅ UI открылся за `10.7 сек`, таблица есть, 136 строк.
- ✅ Текущий режим: `V2 Динамика`, view mode `Кораба`.
- ✅ Показаны недельные заказы, тренд, V1/V2, R², множитель.
- ✅ `Учитывать отгрузки` отображается, выбранных поставок `0`.
- ✅ Кнопка `Сформировать отгрузку` есть.

Статус: ✅ работает.

#### 4.2 Товары

Проверка:

- ✅ Внутренний таб открылся за `11.0 сек`, таблица есть.
- ✅ Показывает 63 товара из боевого источника.
- ✅ Видны остатки, заказы, размеры по ключевым артикулам.

Статус: ✅ работает.

#### 4.3 Загрузка данных

Проверка:

- ✅ Внутренний таб открылся за `7.8 сек`.
- ✅ Показывает почасовое автообновление с галочками.
- ✅ Текущие данные: заказы `35 818`, остатки `28 494`, товары `63`.
- ✅ Последняя загрузка: `25.07.2026, 11:24:00`.
- ✅ Cron `shipment-sync.sh` в 11:24 МСК завершился `stockSkipped:false`, stock rows `804`.
- ⚠️ Мутацию `Загрузить всё из WB` не запускал в рамках аудита.

Статус: ✅ работает, данные актуальны.

#### 4.4 Настройки отгрузки

Проверка:

- ✅ Внутренний таб открылся за `8.2 сек`, таблица выкупа есть.
- ✅ Режим выкупа: `auto`.
- ✅ Режим регионов: `auto`.
- ✅ `uploadDays=28`, `warehousePackingDays=30`, `warehousePackingMultiplier=1`.
- ✅ Таблица реального % выкупа по артикулам отображается.
- ⚠️ Сохранение настроек не выполнялось.

Статус: ✅ работает.

### 5. Расчёт логистики

Назначение: расчёт стоимости логистики/хранения по артикулам и складам, объёмы из карточек/остатков/замеров, индексы локальности.

Источники:

- `/api/logistics/products`
- `/api/logistics/alerts`
- `/api/logistics/tariffs`
- `/api/settings`
- PostgreSQL: `shipment_products`, `shipment_stock`, `warehouse_remains_volume`, `warehouse_measurements`, `logistics_tariff_cache`, `shipment_orders`.

Проверка:

- ✅ UI открылся за `5.2 сек`, таблица есть, 63 строки.
- ✅ API products: 278 rows, meta есть.
- ✅ Индекс локализации UI: `1.00`.
- ✅ Заказы в расчёте UI: `112 350`.
- ✅ Локальность отчёта WB UI: `64.6%`.
- ✅ Remains volume fresh: `2026-07-25T06:00:11Z`.
- ✅ Measurements fresh: `2026-07-25T06:10:01Z`.
- ⚠️ `measurementOverCardCount=8`, новые замеры за 7 дней `0`.

Статус: ✅ работает; ⚠️ есть 8 бизнес-алертов по объёму.

### 6. Реклама

Назначение: внешний рекламный дашборд WB Ads.

Источник:

- Внешняя ссылка в меню: `https://ads.imaxprom.site`.

Проверка:

- ✅ С production-сервера домен отвечает `HTTP/2 401`.
- ✅ Это ожидаемо: сервис живой и закрыт Basic Auth.
- ⚠️ Локальная среда аудита не смогла резолвить домен за 15 сек, но production-сеть доступность подтвердила.

Статус: ✅ внешний сервис доступен, защищён авторизацией.

### 7. Склад

Назначение: готовые короба/остатки склада из Google Sheets, план упаковки, тренд и детализация по размерам.

Источники:

- `/api/warehouse/stock`
- `/api/warehouse/sync`
- `/api/settings`
- Google Sheets через service account.
- PostgreSQL: `warehouse_ready_stock`, `warehouse_sync_runs`, `shipment_orders`, `shipment_stock`.

Проверка:

- ✅ UI открылся за `8.5 сек`, таблица есть.
- ✅ API warehouse-stock: 127 rows, 19 articles.
- ✅ Готово: `15 369 шт`, `177.12 коробов`.
- ✅ Последний импорт UI: `25.07, 11:38`.
- ✅ Общий тренд отображается `×0.44`.
- ✅ Кнопка `Обновить` есть.
- ⚠️ Ручную синхронизацию не запускал.

Статус: ✅ работает, данные актуальны.

### 8. Поставки

Назначение: список поставок WB, статусы, склад, упаковано/принято, детализация состава.

Источники:

- `/api/supplies?limit=20`
- `/api/supplies/[id]/package`
- WB Supplies API.
- PostgreSQL cache: `wb_supply_snapshots`, `wb_accepted_supplies`, accepted supply content.

Проверка:

- ✅ UI открылся за `4.3 сек`, таблица есть, 19 строк.
- ✅ Показывает последние поставки, включая `41080701`, `41063617`, `41012038`, `40940961`.
- ✅ Склады отображаются: Рязань, Тула, Новосемейкино, Екатеринбург и т.д.
- ✅ Статусы отображаются: Принято, Запланировано, Идёт приёмка, Отгрузка разрешена.
- ✅ Упаковано/принято отображается.
- ⚠️ API `/api/supplies?limit=20` занял `29.9 сек`. Это главный риск вкладки: при холодном кэше пользователь может ждать слишком долго.

Статус: ✅ работает; ⚠️ нужно оптимизировать скорость.

### 9. Отчёты поставок

Назначение: архив документов по принятым поставкам: акт приёмки, отчёт сверки, Честный знак; скачивание с нашего сервера.

Источники:

- `/api/supply-reports`
- `/api/supply-reports/sync`
- `/api/supply-reports/[supplyID]/documents/[type]`
- PostgreSQL: `wb_accepted_supplies`, `wb_supply_report_documents`.
- Файлы: `data/supply-documents/<supplyID>/`.
- WB Seller Portal endpoints и WB documents-api.

Проверка:

- ✅ UI открылся за `4.7 сек`, таблица есть, 25 строк.
- ✅ Всего accepted supplies: `308`.
- ✅ Документы:
  - `acceptance_act`: saved `285`, error `23`;
  - `reconciliation_report`: saved `285`, error `23`;
  - `honest_sign`: saved `129`, missing `23`.
- ✅ Последний daily cron `2026-07-25 08:00 МСК` отработал: `checkedSupplies=100`, `savedDocuments=277`, `missingDocuments=23`, `errors=[]`.
- ✅ Верхние 25 строк: `68` сохранённых документов, `7` missing, `0` errors.
- ✅ Пример `41080701`: акт saved, сверка saved, Честный знак missing.
- ✅ Пагинация и лимит 25 строк работают.

Статус: ✅ работает, daily cron работает.

### 10. Закупки

Назначение: расчёт сырья к закупке по артикулам/категориям с учётом готового WB-товара, заказов, Google Sheets остатков сырья.

Источники:

- `/api/data/products`
- `/api/data/stock`
- `/api/data/orders-aggregated?days=30`
- `/api/purchases/stock`
- Google Sheets: purchase stock + warehouse article configs.

Проверка:

- ✅ UI открылся за `5.1 сек`, таблицы есть.
- ✅ API purchases-stock: `ok=true`, 3 листа, 19 warehouse article configs.
- ✅ Боевой источник: `63 товаров`.
- ✅ UI показывает:
  - готовый WB учтён `100 510`;
  - цель 30 дней сырьё `136 130`;
  - нужно сырья `47 806`;
  - сырьё на складе `47 322`;
  - к закупке `484 шт / 41 пачка`.
- ✅ Таб `Настройки` открылся за `4.5 сек`, таблица 63 строки.
- ⚠️ Многие товары в настройках `Не участвует` и `Состав не задан`; это может быть штатно для нерелевантных артикулов, но для новых продаваемых артикулов требует контроля.

Статус: ✅ работает.

### 11. Мониторинг

Назначение: состояние PM2, cron, data-health, логи, ошибки, auth summary.

Источники:

- `/api/monitor/status`
- `/api/monitor/data-health`
- `/api/monitor/summary`
- `public/data/monitor/status.json`
- `public/data/monitor/data-health-cron.json`
- production logs in `data/*.log`.

Проверка:

- ✅ UI открылся за `3.5 сек`, таблица 13 сервисов.
- ✅ PM2 `mphub` online.
- ✅ Большинство cron работают.
- ✅ Data-health содержит 36 проверок.
- ❌ Overall data-health: `error`.
- ❌ Единственная красная проверка: `cron_reviews_complaints`.
- ✅ Фактический `reviews-complaints.log` свежий: `2026-07-25T08:30:01Z`.
- ❌ Причина ложной красноты: текущий штатный лог `No accounts with auto_complaints enabled` не засчитывается как OK.
- ⚠️ UI summary пишет `API — / ЛК — не проверялось`, хотя `auth-check.log` пишет `All channels ok`.
- ⚠️ Console на странице мониторинга: 3 ресурса `404`; вероятно, отсутствующие `/data/monitor/changes.json` и/или `/data/monitor/repair-log.json`. UI не падает, но консоль загрязняется.

Статус после фикса 2026-07-25: ✅ вкладка открывается, `data-health` пересчитан на production и показывает `overall=ok`.

### 12. Журнал

Назначение: история изменений и backlog проекта.

Источник:

- `/data/changelog.json`, файл `public/data/changelog.json`.

Проверка:

- ✅ После фикса 2026-07-25 `/data/changelog.json` на production отдаёт `200`, размер `25 741` байт.
- ✅ Файл `public/data/changelog.json` загружен в текущий release.
- ✅ Локально файл есть: `public/data/changelog.json`.

Статус после фикса 2026-07-25: ✅ восстановлен на production.

### 13. База знаний

Назначение: справочник по разделам, API, бизнес-логике, инфраструктуре.

Источник:

- `/data/docs.json`, файл `public/data/docs.json`.

Проверка:

- ✅ UI открылся за `3.5 сек`.
- ✅ JSON валидный.
- ✅ Разделы отображаются.
- ✅ В базе знаний есть актуальная запись по daily cron отчётов поставок.
- ⚠️ Так как база знаний большая и местами историческая, источником истины для данных всё равно остаются production БД/код/логи.

Статус: ✅ работает.

### 14. Настройки

Назначение: WB API-ключ, проверка доступов, WB Seller авторизация, выход из аккаунта.

Источники:

- `/api/settings/apikey`
- `/api/wb/test`
- `/api/wb/auth`
- `/api/monitor/auth-status`
- Файлы `data/wb-api-key.txt`, `data/wb-tokens.json`.

Проверка:

- ✅ UI открылся за `4.6 сек`, API ошибок нет.
- ✅ API-ключ есть, masked `••••••••••••mivgm5uQ`.
- ✅ Доступы: `12 из 12`.
- ✅ WB Seller session active.
- ✅ Кабинет: `ИП Беликова А. Л.`, телефон `+7 (964) 152-16-52`.
- ✅ `auth-check.log`: `API: ok`, `LK: ok`, `All channels ok`.
- ✅ После фикса 2026-07-25 `/api/monitor/auth-status` доступен странице настроек и отдаёт `api=ok`, `lk=ok`, `checkedAt=2026-07-25T19:46:04.810Z`.
- ⚠️ Кнопки `Удалить` и `Выйти` не нажимались.

Статус после фикса 2026-07-25: ✅ основной функционал работает; строка автопроверки обновлена.

## Скрытые/служебные страницы

- `/finance/settings/test`, `/warehouse/test`, `/shipment/products-test`, `/debug`, `/upload`, `/products`, `/reports` существуют в коде, но не являются основными пунктами левого меню.
- В рамках этого аудита основной акцент был на видимых вкладках и их внутренних табах. Служебные страницы требуют отдельного технического аудита, если планируется ими пользоваться.

## Итоговый список задач

1. ✅ Восстановить `public/data/changelog.json` на production или изменить `/changelog`, чтобы он читал существующий источник. Закрыто 2026-07-25, production HTTP `200`.
2. ✅ Исправить monitor data-health для `reviews-complaints`: считать `No accounts with auto_complaints enabled` штатным OK, если автожалобы выключены. Закрыто 2026-07-25, `data-health overall=ok`.
3. ✅ Проверить `auth-status.json`: auth-check фактически успешен, но UI пишет `не проверялось`. Закрыто 2026-07-25, endpoint открыт для read-only статуса на странице настроек.
4. ✅ Оптимизировать `/api/supplies?limit=20`: закрыто 2026-07-25. Обычное открытие теперь читает актуальный snapshot из БД (`0.123 сек` на production), кнопка `Обновить` делает принудительный live-запрос WB (`26.67 сек`). Количество и первые ID после фикса совпали: `19` поставок, первая `41080701`.
5. ⚠️ Закрыть 19 баркодов без себестоимости. Проверено 2026-07-25: это активные остатки WB на `954 шт`, бренд `BiiZ`; уже есть `17` заказов, но продаж в `realization` пока `0`, поэтому фактическая прибыль ещё не искажена, но риск появится при выкупах.
6. ✅ Разобрать 8 критичных логистических замеров WB: 2026-07-25 критерий временно сужался до товаров с остатком и материальным превышением, но 2026-07-26 по бизнес-решению возвращена старая логика контроля риска. Если последний WB-замер выше карточки, alert остаётся красным независимо от текущего остатка и без порога превышения. Production после возврата показывает `8` critical alert-ов.
7. ✅ Убрать/переименовать legacy-кнопку `Полная загрузка` в подключении отзывов: закрыто 2026-07-25. API `sync=full` в PostgreSQL-режиме отключён, поэтому UI больше не показывает кнопку, которая приводила к `409 reviews_sync_disabled_pg`; вместо неё отображается серверное автообновление.

## Общий вывод

Сайт в целом рабочий: ключевые бизнес-разделы открываются, API отвечают, данные свежие, hourly/daily cron-и выполняются. Операционные проблемы с `/changelog`, ложным красным статусом автожалоб и строкой `auth-status` закрыты 2026-07-25, скорость `/api/supplies?limit=20` исправлена, логистические alert-ы 2026-07-26 возвращены к старой риск-логике, legacy-кнопка полной загрузки отзывов удалена из UI в release `20260725-201138`. Открытым остаётся бизнес-пункт: заполнить себестоимость по 19 активным barcode.

## Продолжение после аудита

### Себестоимость: 19 активных barcode без COGS

Проверка production БД 2026-07-25:

- Источник остатков: `shipment_stock`.
- Источник себестоимости: `cogs`.
- Условие: `shipment_stock.quantity > 0` и `cogs.cost` отсутствует или `<= 0`.
- Результат: `19` barcode, `954 шт` активного WB-остатка.
- Заказы по этим barcode уже есть: `17`.
- Продаж в `realization` пока нет: `0 шт`, `0 ₽`.

Крупнейшие позиции без себестоимости:

| Barcode | Артикул WB | Артикул продавца | Остаток |
|---|---:|---|---:|
| 2053458177839 | 1267055950 | черный_бежевый_сетка | 150 |
| 2053382846146 | 1266604919 | черный_сетка | 116 |
| 2053458275719 | 1267077467 | фиолетовый_молочный_сетка | 88 |
| 2053457901787 | 1267015841 | голубой_сетка | 87 |
| 2053458559857 | 1267100490 | черный_сетка_8в1 | 60 |

Вывод: это реальные активные товары, а не технические строки. Сейчас они ещё не бьют по фактической прибыли, потому что продаж по ним нет, но при первых выкупах себестоимость будет считаться как `0`, и прибыль будет завышена. Нужно заполнить COGS в `Финансы -> Себестоимость`.

### Логистика: критичные WB-замеры

Проверка production API `/api/logistics/alerts` 2026-07-26:

- 2026-07-25 временно было `6` alert-ов после фильтра по текущему остатку и минимальному превышению.
- 2026-07-26 по бизнес-решению возвращена старая логика: `8` критичных alert-ов.
- Причина: даже если остатка сейчас нет, товар может появиться на WB позже; при завышенном замере перезамер нужно инициировать заранее, иначе логистика будет завышена.
- Текущий критерий: WB-замер больше карточного объёма, отчёт остатков отсутствует или тоже показывает объём выше карточки. Текущий остаток и размер превышения не фильтруют alert.

Текущие alert-ы:

| Артикул WB | WB-замер | Карточка | Остаток |
|---:|---:|---:|---:|
| 1012324217 | 2.100 л | 1.500 л | 1 754 |
| 218225554 | 6.048 л | 5.145 л | 0 |
| 304049984 | 2.625 л | 2.500 л | 2 386 |
| 322000486 | 2.625 л | 2.500 л | 3 456 |
| 333768800 | 2.520 л | 2.500 л | 5 |
| 398657691 | 2.500 л | 2.400 л | 7 710 |
| 580062620 | 1.575 л | 1.500 л | 639 |
| 770762506 | 1.890 л | 1.500 л | 348 |

### Отзывы: legacy-кнопка полной загрузки

Проверка 2026-07-25:

- `/api/reviews?sync=full` в PostgreSQL-режиме штатно отключён и возвращает `409 reviews_sync_disabled_pg`.
- Кнопка `Полная загрузка` в UI вводила в заблуждение, потому что запускала отключённый endpoint.
- После исправления production source и страницы `/reviews`, `/reviews/connection` не содержат текст `Полная загрузка` и не вызывают `sync=full`.
- Статус базы отзывов продолжает читаться через `/api/reviews/sync-status`; новые отзывы остаются на серверной синхронизации.
