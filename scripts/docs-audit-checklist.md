# Чек-лист актуализации базы знаний MpHub

> Этот документ — инструкция для LLM-агента по полной проверке и актуализации базы знаний.
> Выполняй строго по порядку. Не пропускай ни одного шага.

---

## КРИТИЧЕСКИЕ ПРАВИЛА

1. **ЗАПРЕЩЕНО использовать память, кэш, предыдущий контекст.** Каждый файл открывай и читай заново через Read/cat. Даже если ты "помнишь" содержимое — перечитай.
2. **Источник истины — production + код + БД.** Для операционных вещей сначала проверяй `ssh wb-site`: PM2, crontab, `/home/makson/website`, runtime-файлы. Для логики приложения — код. Документация = то, что нужно привести в соответствие.
3. **Один раздел за раз.** Не переходи к следующему разделу, пока не закончил текущий. Фокус на одном.
4. **После каждого изменения — проверка.** Минимум: валидность JSON + `npm run build`. API smoke — по задаче и только если не трогает production-данные.
5. **Не меняй код приложения в рамках аудита знаний.** Разрешённые файлы: `public/data/docs.json`, `CLAUDE.md`, `docs/*.md`, `REFACTORING.md`, `public/data/mphub-database-guide.md`, `scripts/*.md`. Если нашёл баг в коде — запиши в отчёт, но не исправляй без отдельного решения.

---

## ШАГ 0: Подготовка

### 0.1 Зафиксировать текущее состояние
```bash
cd /Users/octopus/Projects/website
git status
git log --oneline -5
```
Запомни хеш последнего коммита — это точка отката.

### 0.2 Проверить production baseline
```bash
ssh wb-site 'cd ~/website && pm2 status mphub --no-color && crontab -l'
```
Зафиксируй: PM2 process, production crontab, project path, health-check URL.

### 0.3 Прочитать структуру docs.json
```bash
python3 -c "
import json
with open('public/data/docs.json') as f:
    d = json.load(f)
for s in d['sections']:
    blocks = len(s.get('blocks',[]))
    print(f\"{s['id']:30s} | {s['title']:40s} | {blocks} blocks\")
"
```
Запиши список всех разделов. Ты пройдёшь каждый.

---

## ШАГ 1: Проверка раздела (повтори для КАЖДОГО раздела)

### 1.1 Определи файлы раздела
Для текущего раздела (например `analytics`):

```bash
# Найди страницу
ls src/app/РАЗДЕЛ/

# Найди компоненты модуля
ls src/modules/РАЗДЕЛ/ 2>/dev/null

# Найди API
ls src/app/api/РАЗДЕЛ/ 2>/dev/null
```

### 1.2 Прочитай код страницы ПОЛНОСТЬЮ
```bash
# Читай ВЕСЬ файл, не первые 20 строк
cat src/app/РАЗДЕЛ/page.tsx
```

Запиши:
- [ ] Какие импорты (откуда берутся компоненты, хуки, утилиты)
- [ ] Какие данные загружаются (fetch, useData, useAnalyticsData и т.д.)
- [ ] Какие UI-элементы на странице (карточки, таблицы, графики, вкладки)
- [ ] Какие API вызываются
- [ ] Есть ли layout.tsx и error.tsx

### 1.3 Прочитай каждый компонент раздела
Для каждого компонента, импортированного на странице:
```bash
cat src/modules/РАЗДЕЛ/components/КОМПОНЕНТ.tsx
# или
cat src/components/КОМПОНЕНТ.tsx
```

Запиши:
- [ ] Что компонент делает
- [ ] Какие props принимает
- [ ] Какие API вызывает
- [ ] Откуда берёт данные

### 1.4 Прочитай API-роуты раздела
```bash
cat src/app/api/РАЗДЕЛ/РОУТ/route.ts
```

Запиши:
- [ ] HTTP метод (GET/POST/PUT/DELETE)
- [ ] Параметры запроса
- [ ] Что возвращает
- [ ] Откуда берёт данные (какая таблица, какой SQL)

### 1.5 Прочитай lib-модуль раздела (если есть)
```bash
cat src/modules/РАЗДЕЛ/lib/*.ts
```

Запиши:
- [ ] Какие функции экспортируются
- [ ] Какие зависимости (другие модули? shared?)

### 1.6 Проверь, есть ли кросс-импорты
```bash
grep -r "from.*modules/" src/modules/РАЗДЕЛ/ --include="*.ts" --include="*.tsx" | grep -v "from.*modules/РАЗДЕЛ/"
```
Если есть импорты из ДРУГОГО модуля — это нарушение. Запиши.

---

## ШАГ 2: Сверка с docs.json

### 2.1 Прочитай текущее описание раздела в docs.json
```bash
python3 -c "
import json
with open('public/data/docs.json') as f:
    d = json.load(f)
for s in d['sections']:
    if s['id'] == 'РАЗДЕЛ':
        print(json.dumps(s, ensure_ascii=False, indent=2))
"
```

### 2.2 Сверь КАЖДЫЙ блок

Для каждого блока в разделе:

**Тип `text` (описание):**
- [ ] Описание соответствует тому, что ты увидел в коде?
- [ ] Источник данных указан правильно?
- [ ] Упомянуты ли Error Boundary, провайдеры, модульная структура?

**Тип `list` (списки элементов):**
- [ ] Каждый элемент списка существует в реальности?
- [ ] Нет ли элементов, которых уже нет? (удалённые табы, переименованные компоненты)
- [ ] Нет ли пропущенных элементов? (новые API, новые файлы, новые карточки)
- [ ] Пути файлов корректны? (src/components/ vs src/modules/РАЗДЕЛ/components/)

**Тип `schema` (потоки данных):**
- [ ] Поток данных соответствует реальному коду?
- [ ] API-эндпоинты в потоке существуют?
- [ ] Количество элементов на выходе правильное? (4 карточки vs 5)

**Тип `code` (исходный код):**
- [ ] Описание в title актуально?
- [ ] Путь файла правильный?

### 2.3 Проверь пути файлов
Для КАЖДОГО файла, упомянутого в docs.json:
```bash
test -f "ПУТЬ" && echo "✅ EXISTS" || echo "❌ MISSING: ПУТЬ"
```
Если файл не существует — удали из docs или исправь путь.

### 2.4 Проверь API-эндпоинты
Для каждого API, упомянутого в docs.json:
```bash
test -f "src/app/api/ПУТЬ/route.ts" && echo "✅" || echo "❌ MISSING"
```

---

## ШАГ 3: Внесение правок

### 3.1 Исправь docs.json программно
Используй Python для точечных правок:
```python
import json
with open('public/data/docs.json') as f:
    d = json.load(f)

for s in d['sections']:
    if s['id'] == 'РАЗДЕЛ':
        # Конкретная правка
        s['blocks'][N]['items'][M] = "новое значение"

with open('public/data/docs.json', 'w') as f:
    json.dump(d, f, ensure_ascii=False, indent=2)
```

### 3.2 Проверь локальную сборку
```bash
npm run build
```
`scripts/rebuild-server.sh` не использовать как production deploy; это legacy/local helper. Production deploy описан в `CLAUDE.md` и `docs/production-network.md`.

---

## ШАГ 4: Проверка других документов

После docs.json проверь эти файлы (тем же методом — читай, сверяй с кодом):

### 4.1 mphub-database-guide.md
```bash
cat public/data/mphub-database-guide.md
```
Для КАЖДОЙ таблицы:
```bash
sqlite3 data/finance.db ".schema ИМЯ_ТАБЛИЦЫ"
```
- [ ] Все колонки совпадают?
- [ ] Все индексы указаны?
- [ ] Нет ли таблиц в БД, которых нет в документе?
```bash
sqlite3 data/finance.db ".tables"
sqlite3 data/weekly_reports.db ".tables"
```

### 4.2 REFACTORING.md
```bash
cat REFACTORING.md
```
- [ ] Пункты "Выполнено" реально выполнены? (проверь в коде)
- [ ] Пункты "не выполнено" реально не выполнены?
- [ ] Статистика кодовой базы актуальна?

### 4.3 e2e-checklist.md
```bash
cat scripts/e2e-checklist.md
```
- [ ] Описание выкупа соответствует реальному источнику данных?
- [ ] Количество вкладок правильное?
- [ ] UI-проверки соответствуют текущему интерфейсу?

### 4.4 CLAUDE.md и production docs
```bash
sed -n '1,220p' CLAUDE.md
sed -n '1,220p' docs/production-network.md
```
- [ ] Стек технологий актуален?
- [ ] Структура проекта соответствует реальной?
- [ ] Production описан как `PM2 + cron + VPS`, а не как macOS `launchd`?
- [ ] Deploy описан через `scripts/deploy.sh` + `scripts/prod-safe-build.sh`?
- [ ] Health-check с VPS указан как `http://127.0.0.1`, а не публичный HTTPS-домен?

---

## ШАГ 5: Финальная проверка

### 5.1 Полный прогон тестов
```bash
npm run build
```

### 5.2 Проверка JSON валидности
```bash
python3 -c "import json; json.load(open('public/data/docs.json')); print('docs.json: valid')"
```

### 5.3 Коммит
```bash
git add -A
git diff --cached --stat
git commit -m "docs: актуализация базы знаний по чек-листу

Проверены разделы: [перечисли]
Исправлено: [кратко]
Тесты: 9/9 MATCH

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

## Порядок проверки разделов

Проходи разделы в этом порядке (от самых изменяемых к стабильным):

1. `analytics` — Аналитика
2. `finance` — Финансы
3. `shipment` — Расчёт отгрузки
4. `reviews` — Отзывы
5. `monitor` — Мониторинг
6. `lib` — Бизнес-логика
7. `components` — Компоненты
8. `api-routes` — API-маршруты
9. `scripts` — Скрипты
10. `codes` — Исходный код (заголовки)
11. `databases` — Базы данных
12. `overview` — Обзор
13. `deploy` — Деплой
14. `agents` — Агенты
15. `pages` — Страницы
16. Остальные разделы

---

## Формат отчёта

После завершения предоставь отчёт:

```
ОТЧЁТ ОБ АКТУАЛИЗАЦИИ БАЗЫ ЗНАНИЙ
Дата: YYYY-MM-DD
Коммит до: HASH
Коммит после: HASH

ПРОВЕРЕНО РАЗДЕЛОВ: N из M
ИЗМЕНЕНО БЛОКОВ: N
ДОБАВЛЕНО: N элементов
УДАЛЕНО: N элементов
ИСПРАВЛЕНО: N элементов

ДЕТАЛИ:
- analytics: [что изменено]
- finance: [что изменено]
- ...

НАЙДЕННЫЕ ПРОБЛЕМЫ В КОДЕ (не исправлены):
- [описание]

ТЕСТЫ: 9/9 MATCH
```
