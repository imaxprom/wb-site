# MpHub — Environment Variables
# Copy this file to .env.local and fill in values

# JWT secret for auth tokens (REQUIRED in production)
JWT_SECRET=

# WB API key (alternatively stored in data/wb-api-key.txt via UI)
WB_API_KEY=

# Node environment
NODE_ENV=development
